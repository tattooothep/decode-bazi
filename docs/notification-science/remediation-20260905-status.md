# Notification science remediation — 2026-09-05

Goal is ACTIVE. No production change or new release is approved by this ledger.

## Baselines and constraints

- Backend working branch: `fix/notification-science-20260905`, starting at `23988a39e37caa8a5d3c7fb2ff9502b538739c43`.
- Current deployment: `/root/releases/decode-app-r573-network-morning-final`; `.release-commit` is `6ebeb3b9be2c95156959717ca2e24d66119fc0ec`.
- The old R8 five-signature evidence applies to its hard-off artifact only. It is not approval of these changes, not physical delivery proof, and not a source-complete Qizheng release.
- Keep user preferences, account/profile ownership, historical snapshots and other sciences intact. No shared production credential changes, history rewrites, unregistered test recipients, or blanket device disablement.
- Five independent final reviews must bind exact code/artifacts and cover science/time, understandable Type-C localization, device delivery, security/non-regression, and release evidence. Authors cannot sign their own work.

## Work streams

- [ ] Qimen: dynamic month-dependent nine-star vigor; separate sourced door rules; source/version/wire compatibility; explain seven-day silence without weakening filters.
- [ ] Delivery: diagnose and repair Expo InvalidCredentials while preserving working FCM and legitimate iOS devices; prove account/install ownership and current receipt semantics.
- [ ] Ziwei: human-readable Type-C notification and history in all nine supported locales, retaining inspectable raw names; never discard meaning to fit a byte cap or describe constant contextual stars as selective auspicious events.
- [ ] Zibai: continuous apparent-solar clock at leap-year seams, half-open interval regression tests, no retrospective history regeneration.
- [ ] R8: reconcile APK/backend/database/runbooks under existing migration and activation gates; source-incomplete Qizheng remains visibly unavailable, not reported complete.
- [ ] Integration: build, staged deployment/rollback, real eligible notification and detail-opening evidence, five independent final signatures.

## Evidence already confirmed by read-only audit

- Qimen chart 1062 caches `火土木水金`, while the declared Yanbo nine-star rule requires `木火水金土` in 午 month. Door tradition must be established independently; do not assume nine-star rules apply to doors.
- Expo had 14 dead attempts with InvalidCredentials over seven days; FCM had accepted Zibai/Ziwei deliveries. Root cause of Expo failures has not yet been established.
- Thai Ziwei fallback removes the palace topic and leaves raw Chinese tokens. Existing Type-C fixture checks size and label presence, not comprehension.
- At UTC `2028-01-01T00:00:00.000Z`, longitude `-14.33010681505132`, Zibai throws `zibai_snapshot_active_interval_mismatch`. Annual fractional phase reverses by about 36 ms.
- The deployed backend has neither R8 source/routes nor its database producer-state relation.

## Current progress

- Previous goal-setup turn created the active goal but made no implementation progress.
- 2026-09-05: revalidated deployment/source baselines and created the dedicated branch in the existing isolated worktree. Independent Qimen source and Expo root-cause investigations are running. Zibai repair is independently testable while those critical investigations proceed.
- Zibai source fix committed as `088374b214cd8f61b10750688bd88d145f375d35`; author recorded exact pre-fix failure and passing focused/regression suites. Independent review approved SPEC and QUALITY in `remediation-20260905-zibai-review.md`, with an analytic correction bound under 47.25 ms and broader seam/interval checks. This is task-level approval only; deployment and final reviews remain pending.
- Qimen: `scripts/test-qimen-seasonal-vigor.mts` now reproduces the actual advisory defect (`休 !== 旺`) on a complete June witness. It remains intentionally failing until integration is repaired. A pure, source-fingerprinted nine-star helper and 60-cell explicit oracle pass in `test-qimen-nine-star-month-rule.mts`; the helper is NOT yet connected to the producer or mobile. Current mobile rejects 廢, so coordinated semantic version/decoder/rendering work is required before rollout.
- Qimen door method: primary corpus contains several distinct rules; an asynchronous user question asks whether to retain the documented ordinary-five-element door policy or select Tongzong 暗餘氣. No production door method changes have been made. Nine-star work is not dependent on inventing an answer.
- Expo: independent report `audit-20260905-expo-root-cause.md` establishes legacy Android Expo-only routing, not duplicate registration rows. The external EAS Android FCM credential is the failure plane; no authenticated EAS session/CLI/EXPO_TOKEN exists on this host. Repair requires project credential authority; do not export the local FCM key without that authority. Platform-specific monitoring and credential-error reporting still need a code fix. No registrations were disabled.
- Ziwei: source inspection confirms the full V2 snapshot nearly exhausts the provider envelope and the 360-byte fallback removes meaning. Retain strict provider-size safety and all offline snapshot/detail information; do not merely raise the byte cap or substitute raw Chinese for explanations. Lossless compact transport plus compatible client registration, or a genuinely readable bounded copy proven against all nine locales, still needs implementation. No Ziwei source was changed this turn.
- User reiterated no Superpower/extra brainstorming. Continue direct implementation and necessary checks, with no additional workflow ceremony. Keep all five work streams and release acceptance requirements intact.
- Delivery health patch committed as `a0ae1b3b3ccb6659963f36774ec2a86bd7cc95ec`: readiness is now FCM / Expo iOS / Expo Android, and recent InvalidCredentials causes an explicit unhealthy reason even with readiness attestations. Parent reviewed the code and independently reran all four focused pure suites successfully. This detects the credential failure; it does not repair the external EAS credential or prove phone display.
- Ziwei lossless V3 codec committed as backend `2debc6e00ca9c7f61a8ef628f54238779c8fb9ee` and mobile `09f8658a536395e3df0e1c3d484c835b972e5cce`. Backend default remains V2; explicit `{schema:3}` is required. Strict mobile decoding expands V3 to the unchanged resolved V2 offline-detail schema. Cross-runtime tests preserve every field, all three layers, nulls, and digest; 27-snapshot matrix passes. No enrollment was changed.
- Ziwei readable copy is now implemented opt-in with all nine locales, full transformation meanings, actual target topics/raw names, and separate month/day/hour focuses. New regression captured both the old unreadable Thai fallback and the provider adapter's extra-URL incompatibility, then passed after repair. 126-case readable suite is GREEN (FCM max 3882 B / Expo 3593 B), and cross-runtime integration probe has zero failures. Independent task-level review found no blocking findings. Reports: `remediation-20260905-ziwei-readable-report.md` and `remediation-20260905-ziwei-readable-review.md`.
- Next Ziwei work is coordinated schema-3 enrollment/migration, per-installation scheduler selection, reservation/retry compatibility and release evidence. None of the above is active on production yet; goal and all final release gates remain open.
- Ziwei readable copy/adapter bridge committed as `5fb6547`. Backend compatibility enrollment committed as `6225a5216dec97386f671047baa795b8f82d2671`, with fail-fast migration follow-up `71d472741744ff9600e0381fa616cbbd0228034a`; migration has NOT run. Mobile enrollment is committed as `0e95e761e249d2c17771be02f89f6a976d51972c`; parent independently reran its 47-client-case suite plus capability/wiring/codec checks. The APK has NOT been built or released.
- Ziwei per-installation scheduler and V2/V3 reservation/retry integration is implemented and GREEN on focused tests. It preserves sealed V2 attempts on token upgrade and rejects V3 on downgrade, binds public copy to the locked account locale, and recomputes exact V3 meaning from the persisted snapshot + historical locale. An independent review caught the matched-history/provider-text forgery gap; an explicit RED fixture reproduced it before the final semantic-copy fence was added. Current in-memory reservation matrix passes 72 combinations. See `remediation-20260905-ziwei-delivery-report.md` for evidence and deployment limits.
- No production migration, service/environment/credential mutation, test push, device installation, deployment or final signature was performed during these changes. Remaining validation includes disposable-database integration, full release builds, compatible staged rollout/rollback, physical receipt/detail proof and all final reviews. Earlier schema-3 registration-only commits with a schema-2-only scheduler are not safe rollback floors.
- Qimen 2026-09-05 follow-through: canonical month-dependent V4 advisory, separate explicit door profiles, exact seasonal provenance, full-grid occurrence builder and opt-in schema4 scheduler are now implemented and pass focused RED→GREEN regressions. June1062 witness now has 天輔旺 under the new method, without overriding weak-door or other safety gates. Boundary evidence committed as `a8fbe23`; backend V4 contract/detail committed as `919e56a7f1c7ea804da4c1a2cba493abb881194e`, with frozen historicalV2/V3 readers. See `remediation-20260905-qimen-seasonal-integration.md` for tests and remaining gates. This supersedes the earlier intentionally-RED/helper-only status, not the production status: no new enrollment/method selection/migration/deployment has occurred.
- Subsequent Qimen validation: V4 contracts now reject non-travel purpose and weak selected star/door pairs (`eb3dc34`, independent `a310739`). Backend registration/DDL compatibility is staged (`3643aa7`, migration not executed). Mobile strict V4 reading, duplicated-component rejection, nine-locale strength explanations and method legends are committed through `463e5dca`; native capability4 is staged but not released. Seven-day audit `4a152cc` found 32 skipped claims with no provider attempt in that window; exact rejected-chart reasons are not retained, so no claim that all hours were scientifically unusable is justified.
- Qimen durable transport now binds the full occurrence version tuple, strict payload and immutable copy/source on retries. Independent review reproduced a malformed-envelope legacy fallback, then verified the parent fix in 19 public-retry cases (`2dd454d`). The 144-case size review (`548fae9`) corrects an earlier report: prepared-message bytes are not whole HTTP-request bytes, and no runtime 4,000-byte guard exists yet. Conditional-warning size coverage remains to be checked.
- R8 production audit (`23699e2`) confirms missing routes/tables/audience, despite an old signed v233 APK. Mobile preview truth-state and owner/audience fencing are being fixed; these do not activate delivery. Ten Qizheng sources remain pending. Expo project credential authority and explicit door-method choice are still outstanding external inputs; neither is silently substituted.
- Evidence correction: an earlier agent reported the legacy Qimen scheduler test as pure, but its source includes eight engine calculate POSTs with `skip_save`. That result is excluded from no-network evidence; current independent transport regression uses mocks/fetch tripwires. No notification push, migration or deployment has been performed by the current integration pass.
- Latest continuation: mobile capability4 is committed as `dad3b564`; R8 preview truth and owner-bound async state as `94a76161`. Diagnostic Android Hermes export from `dad3b564` passed, but is not an APK and predates the next intrinsic-warning fix. APK build/release, real phone proof and final signatures remain open.
- Independent V4 review found rehashed missing/invented intrinsic warnings accepted despite the canonical producer emitting the correct warning. Parent reproduced 24 cross-runtime invalid acceptances, then added V4-only selected-hour intrinsic consistency checks: all 24 are now rejected while 6 valid controls pass. V2/V3 and other-science focused regressions pass. See `remediation-20260905-qimen-intrinsic-contract-fix.md`; the selected source formulas and producer thresholds are unchanged.
- Real disposable PostgreSQL integration passed 23 checks including exact additive migrations, data preservation, bounded DDL lock rollback, immutable reserve/retry and concurrent workers. It is being rerun after the V4 fixture correction; do not count the earlier result as approval of final code. Synthetic fixture correction separately records 4 no-pair + 16 intrinsically ineligible originals, rather than mislabeling all original month/profile charts eligible.
- Release preflight found the mandatory mobile full suite includes live engine requests and disposable native prebuilds, while its filename-only offline self-test is not evidence of zero networking. The current Unity module is shared through a symlink. Do not run native clean/build blindly or claim the full suite passed from its self-test. Required signing env and authenticated EAS project authority are absent in the audited shell.
- Intrinsic fix committed backend `5f5fa2c`, mobile `1d751099`; test-only fixture correction backend `fc85afc` / mobile `3ef7ba0c` now explicitly rejects 16 internally rebound severe star/door selections in addition to 24 cross-runtime warning-corruption checks. Independent PostgreSQL stable-hash rerun `cbb416e` passes 26 checks with zero external fetches; report correction `6a7fdaa` distinguishes test byte assertions from runtime enforcement. The earlier 23-check/pending status is superseded.
- New diagnostic Android Hermes export from `1d751099` passes and source-map content exactly matches the current intrinsic guard (`7e9e39f56798cd53a8d0be24551f86747ff23ab7ab0692debc548a07ab28c8b1`). Mobile evidence/preflight committed as `3e16750c`. No new APK, full native suite, installation, production deployment, external credential repair or final goal signature is implied.
- Independent all-component guard review is APPROVED and persisted in `0c1f85a`; parent independently reran its test: 84 old invalid acceptances → 0 current, 18 valid snapshots, 44 provider-build and 44 detail-reader rejection checks, six historical-byte comparisons, zero fetches. This is a bounded implementation approval, not one of five final artifact/phone signatures. The readable provider copy and other-science policy remain untouched by this guard.
- Subsequent release follow-through inspected the real local calculate route/save path, then ran the existing two-case V3 golden under a fetch wrapper fixed to loopback, two exact inputs, `skip_save:true`, no authentication headers and no redirects. Both passed through actual engine → historical builder → current mobile parser/render; both returned null run IDs and matching saved-history rows remained zero. This is explicitly networked local engine evidence, not an offline/full-suite or V4 science pass. No notification provider was called.
- The existing local Android debug certificate was read without key export and matches the established internal upgrade SHA `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c` (mobile `9172bb92`). Thus missing signing variables in the audited shell are not proof the internal key is unavailable; a later scoped internal build can use the matching signer. It is not a Play Store signing approval and does not resolve external Expo project credential authority. Next safe implementation work includes incorporating the omitted remediation build gates and enforcing the known local integration boundary while preserving all legacy tests; native shared-owner isolation, new version/artifact and actual rollout remain pending.
- Later build evidence is recorded separately in the mobile V234 APK report and backend artifact identity report; earlier no-APK/no-build entries are historical, not current build status. A normal built APK does not satisfy the mandatory observed native receipt chain or prove installation/phone delivery.
- Source-only remediation evidence tools committed as `05b9ccf` independently passed review and 54 supplied checks; the fixed policy binds 96 backend/53 mobile deltas and 23 unchanged controls. This is not installed migration preflight or release approval: actual proof validators and all approval flags remain explicitly incomplete/false, with separate frozen application and verifier-tool checkouts.
- Actual isolated R8 test at `2026-09-05T05:15:49.657Z`–`05:16:06.422Z` exited 0: unchanged original assertions, two forward runs, one rollback and seven confirmed PostgreSQL rejections. Only a new network-isolated disposable cluster was used and then removed by verified ID; production remained running. See [the exact receipt, safety review and coverage limits](remediation-20260905-isolated-r8-migration.md). This does not establish full production-schema/runtime-role, observed native build, credential repair, deployment, phone receipt or final five-reviewer approval. Goal remains ACTIVE.
